// Movimento base
move_dir = 270; // Começa em 270 (-1 no seno) para velocidade inicial ZERO (arranque suave)


ancora_x = x + range_x; 
ancora_y = y + range_y;

hsp_decimal = 0;
vsp_decimal = 0;
hsp = 0;
vsp = 0;

alarm[0] = 120;
comeca = false;